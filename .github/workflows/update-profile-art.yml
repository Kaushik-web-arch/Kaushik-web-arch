name: Refresh profile art

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/**"
      - ".github/workflows/update-profile-art.yml"

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout profile repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: scripts/requirements.txt

      - name: Install profile-art dependencies
        run: pip install -r scripts/requirements.txt

      - name: Generate profile art
        run: |
          python scripts/make_ascii_svg.py
          python scripts/make_info_card.py
          python scripts/fetch_contributions.py
          python scripts/render_heatmap_svg.py

      - name: Commit refreshed art
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/kaushik-ascii.svg assets/info-card.svg assets/contrib-heatmap.svg data/contributions.json
          if git diff --cached --quiet; then
            echo "Profile art is already current."
          else
            git commit -m "chore: refresh profile art [skip ci]"
            git push
          fi
